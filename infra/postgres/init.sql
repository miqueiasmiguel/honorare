-- Honorare database initialization
-- Tables are created by EF Core migrations; this file handles
-- extensions and initial setup only.

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "pgcrypto";
